def run_single_user(
    locust_class,
    catch_exceptions=False,
    include_length=False,
    include_time=False,
    include_context=False,
    loglevel=None,
):
    raise Exception("please use locust.run_single_user() instead, now that it is available in locust core")
